[pytest]
# `pythonpath = .` so `import app...` works when pytest runs from backend/.
# Without it pytest puts only backend/tests on sys.path and every import fails.
pythonpath = .
testpaths = tests
addopts = -q --strict-markers
