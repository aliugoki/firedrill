"""The long-running edge process.

Everything else in EVAC-120 is a library: given inputs, produce outputs. This is
the part that decides *when*, and it is the part that has to keep running while
things break around it.
"""
